name: Simple Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  simple-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run simple command
        run: echo "Pipeline is working! This is my first workflow."

      - name: List files in repository
        run: ls -la

      - name: Final step
        run: echo "Workflow completed successfully!"
